/*
 * Author: Chen Zhi <chenzhibupt@qq.com>
 * Date: May 9, 2022
 *
 * Heap Sort
 *
 * 
 * 
 * 
 */

import { init, swap } from '../common/utils';

const heapSort = (arr: number[]): void => {
  // todo
};

const arr = init();
console.log(arr);
heapSort(arr);
console.log(arr);
